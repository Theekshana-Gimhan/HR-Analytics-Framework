# Prevent hoisting of Prisma packages to fix TypeScript resolution
# Prisma client needs to be in the same node_modules as the code using it
legacy-peer-deps=false
install-links=false
# Prevent hoisting @prisma/client to root - each workspace needs its own
hoist-pattern[]=!@prisma/client
hoist-pattern[]=!prisma
