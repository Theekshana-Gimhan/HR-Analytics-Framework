node_modules/
.env
.DS_Store
*.log

# Raw source data - reproducible via scripts/download_datasets.py
data/ibm_hr_attrition.csv
data/raw/

# Derived from gitignored IBM source
data/benchmark_ibm.csv

# Real employee data exported from production DB - never commit
data/local_hr_data.csv

# Python cache
scripts/__pycache__/
__pycache__/
*.pyc

# Trained model binaries (reproducible via scripts/train_model.py)
models/
# Generated training/audit reports/plots (reproducible from scripts/)
reports/*.png
reports/*.json

# Built dissertation (reproducible from created_docs/dissertation/*.md via
# scripts/build_dissertation.py). The Markdown chapters are the source of
# truth; the .docx is a build artifact and would churn on every rebuild.
# The final submission PDF/DOCX gets committed deliberately at hand-in.
created_docs/Final_Report_COM4901_Theekshana_Gimhan.docx

# Playwright Agent CLI working directory. Holds page snapshots, console logs,
# screenshots and browser storage state -- the last of which can contain live
# session tokens for the dev deployment. Never commit.
.playwright-cli/
