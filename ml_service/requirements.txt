# Pinned to the training environment so joblib/pickle unpickles cleanly.
# scikit-learn version MUST match scripts/train_model.py's (1.8.0) — a mismatch
# can silently break or warn on RandomForest unpickling.
scikit-learn==1.8.0
numpy==2.4.1
scipy==1.17.1
joblib==1.5.3
shap==0.52.0
pandas==2.3.3

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
google-cloud-storage==2.19.0
